[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build:client && npm run build:server && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/index.js"
restartPolicyType = "ON_FAILURE"

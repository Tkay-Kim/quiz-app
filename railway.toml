[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd client && npm install && cd .. && npm run build:client && npm run build:server && npx prisma generate"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && node dist/index.js"
restartPolicyType = "ON_FAILURE"
